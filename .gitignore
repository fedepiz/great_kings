# Generated, and not worth keeping. Everything here is rebuilt by ./harness/run-all.sh.
node_modules/
package-lock.json

# the engine bundle the tests import, and the differential's accepted baseline
harness/new.cjs
harness/ref.cjs

# The standalone page. UNLIKE levant-prototype-v25.jsx this one is NOT committed: it is a
# minified React bundle, so every rebuild would rewrite 220KB of unreadable diff, and unlike
# the prototype nothing else reads it. Rebuild with `node build-standalone.js`.
great-kings.html

# scratch copies
*.bak
*.bak*
